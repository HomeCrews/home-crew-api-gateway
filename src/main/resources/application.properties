server.port=8080

spring.application.name=apigateway

spring.cloud.gateway.discovery.locator.enabled=true

# userservice
spring.cloud.gateway.server.webmvc.routes[0].id=userservice
spring.cloud.gateway.server.webmvc.routes[0].uri=lb://userservice
spring.cloud.gateway.server.webmvc.routes[0].predicates[0]=Path=/users/**

# authservice
spring.cloud.gateway.server.webmvc.routes[1].id=authservice
spring.cloud.gateway.server.webmvc.routes[1].uri=lb://authservice
spring.cloud.gateway.server.webmvc.routes[1].predicates[0]=Path=/auth/**

# adminservice
spring.cloud.gateway.server.webmvc.routes[2].id=adminservice
spring.cloud.gateway.server.webmvc.routes[2].uri=lb://adminservice
spring.cloud.gateway.server.webmvc.routes[2].predicates[0]=Path=/admin/**

# bookingservice
spring.cloud.gateway.server.webmvc.routes[3].id=bookingservice
spring.cloud.gateway.server.webmvc.routes[3].uri=lb://bookingservice
spring.cloud.gateway.server.webmvc.routes[3].predicates[0]=Path=/bookings/**

# workerservice
spring.cloud.gateway.server.webmvc.routes[4].id=workerservice
spring.cloud.gateway.server.webmvc.routes[4].uri=lb://workerservice
spring.cloud.gateway.server.webmvc.routes[4].predicates[0]=Path=/workers/**

# notificationservice
spring.cloud.gateway.server.webmvc.routes[5].id=notificationservice
spring.cloud.gateway.server.webmvc.routes[5].uri=lb://notificationservice
spring.cloud.gateway.server.webmvc.routes[5].predicates[0]=Path=/notifications/**

# paymentservice
spring.cloud.gateway.server.webmvc.routes[6].id=paymentservice
spring.cloud.gateway.server.webmvc.routes[6].uri=lb://paymentservice
spring.cloud.gateway.server.webmvc.routes[6].predicates[0]=Path=/payments/**

# xpservice
spring.cloud.gateway.server.webmvc.routes[7].id=xpservice
spring.cloud.gateway.server.webmvc.routes[7].uri=lb://xpservice
spring.cloud.gateway.server.webmvc.routes[7].predicates[0]=Path=/xp/**

# assignmentservice
spring.cloud.gateway.server.webmvc.routes[8].id=assignmentservice
spring.cloud.gateway.server.webmvc.routes[8].uri=lb://assignmentservice
spring.cloud.gateway.server.webmvc.routes[8].predicates[0]=Path=/assignments/**

# ONE import line, not one per profile.
#
# Both application.properties and application-docker.properties are processed
# when the docker profile is active, so an import in each meant a container
# resolved BOTH - including localhost:8888, which is unreachable from inside a
# container. That was harmless only because every import was optional:. With
# fail-fast on it takes the service down. The URI is a placeholder instead, and
# docker-compose.yml sets CONFIG_SERVER_URI in its shared environment anchor.
#
# optional: covers the local case where config-server is not running. It is
# overridden by fail-fast, which compose sets to true: ConfigServerConfigDataLoader
# throws when failFast || !optional, so fail-fast is the real switch.
spring.config.import=optional:configserver:${CONFIG_SERVER_URI:http://localhost:8888}

# Currently INERT, twice over: ConfigClientRetryBootstrapper installs a
# RetryTemplate only when fail-fast is set AND spring-retry is on the classpath,
# and spring-retry is <optional>true</optional> in spring-cloud-config-client so
# it is not transitive. The dependency is deliberately not added yet - it could
# not be resolved offline, and compose already orders clients behind
# `config-server: condition: service_healthy`, so retry is defence in depth
# rather than load-bearing. Add org.springframework.retry:spring-retry (no
# version; managed by spring-cloud-commons-dependencies) to make these live.
spring.cloud.config.retry.max-attempts=12
spring.cloud.config.retry.initial-interval=2000
spring.cloud.config.retry.max-interval=10000
spring.cloud.config.retry.multiplier=1.3
spring.cloud.config.request-read-timeout=10000
spring.cloud.config.request-connect-timeout=5000

# The Eureka URL now comes from home-crew-config (application.yml and
# application-docker.yml). It is deliberately NOT here: leaving a local copy
# would mask a config-server outage, because the built-in Eureka default is
# localhost:8761, which inside a container is nothing - the service would start,
# register nowhere, and look fine.
